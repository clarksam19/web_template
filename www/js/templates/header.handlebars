{{#with new}}
    
  {{#with ../this.update}}
      
    {{#with ../../this.base}}  
      <h1>{{title}}</h1>
      <h2>{{subtitle}}</h2>
    {{else}}
      <h1>No text</h1>
    {{/with}}
  {{else}}
    <h1>Updated Sidebar</h1>
  {{/with}}
{{else}}
  <h1>New Sidebar</h1>
{{/with}}
