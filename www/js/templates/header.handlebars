{{#if (is new)}}
  
{{else}}
  {{#if (is base)}}
    <h1 class="title">{{base.title}}</h1>
    <h2 class="subtitle">{{base.subtitle}}</h2>
  {{else}}
    {{log 'Load Error. Base context is empty'}}
  {{/if}}
  {{#if (is add)}}
  
  {{/if}}
{{/if}}